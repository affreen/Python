 
"""Write one line of Python that takes this list a
and makes a new list that has only the even elements of this list in it."""

def print_even(x):
    for i in x:
        if(i%2==0):
             print(i)
    return

print_even([1, 4, 9, 16, 25, 36, 49, 64, 81, 100])
